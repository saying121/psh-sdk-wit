package profiling:calc@0.0.0;

interface calc-it {
    enum op {
        add,
        sub,
    }
    resource calculator {
        calc: static func(op: op, a: u32, b: u32) -> u32;
    }
}

world imports {
    import calc-it;
}
